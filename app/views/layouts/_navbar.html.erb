<nav class="navbar top-nav" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
    <a class="navbar-item" href="<%= root_path %>">
        <strong>Jazzify</strong>
    </a>
    </div>

    <div class="navbar-menu">
        <div class="navbar-start">
            <!-- navbar items -->
            <%= link_to t('navigation.volunteer_opportunities'), 'volunteer_opportunities_path', class: 'navbar-item' %>
            <%= link_to t('navigation.apply'), 'dashboard_path', class: 'navbar-item' %>
        </div>

        <div class="navbar-end">
            <!-- navbar items -->
            <%= link_to change_language_path, class: 'navbar-item' do %>
              <%= t('navigation.language') %>
            <% end %>
            
            <% if user_signed_in? %>
                <%= link_to logout_path, class: 'navbar-item' do %>
                <%= t('navigation.logout') %>
                <% end %>
            <% else %>
                <%= link_to '/auth/auth0', method: :post, class: 'navbar-item', 'data-turbolinks': 'false' do %>
                <%= t('navigation.auth') %>
                <% end %>
            <% end %>
            
        </div>
    </div>
</nav>